package dto.processors;

import dto.Packet;

import java.lang.reflect.Method;

public class OnePacketProcessor implements Processor {
    @Override
    public Method process(Packet packet) {
        return null;
    }
}
