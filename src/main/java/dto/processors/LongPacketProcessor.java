package dto.processors;

import dto.Packet;

import java.lang.reflect.Method;

public class LongPacketProcessor implements Processor {
    @Override
    public Method process(Packet packet) {
        return null;
    }
}
